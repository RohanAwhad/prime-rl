# M1 Inference Server Config (Frozen Drafter)
# This server generates initial drafts and does NOT receive weight updates.
# Run on port 8003 to avoid conflict with M2 on port 8002.

[server]
port = 8003

[model]
name = "Qwen/Qwen2.5-0.5B-Instruct"
